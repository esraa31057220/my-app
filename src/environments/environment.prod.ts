/** Production build: call API directly (configure host for your deployment). */
export const environment = {
  production: true,
  apiUrl: 'https://your-production-domain.com/api',
  apiServerOrigin: 'https://your-production-domain.com',
};

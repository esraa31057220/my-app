/** Production build: call API directly (configure host for your deployment). */
export const environment = {
  production: true,
  apiUrl: 'https://localhost:7237/api',
  apiServerOrigin: undefined as string | undefined,
};
